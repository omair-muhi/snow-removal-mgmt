<div class="col-md-4">
    <ul class="list-group">
        {{#each employees}}
        {{#unless dataValues.Admin}}
        <li class="list-group-item d-flex justify-content-between align-items-center">
            {{dataValues.id}}.
            {{dataValues.Name}}
        </li>
        {{/unless}}
        {{/each}}
    </ul>
</div>
<div class="col-md-4"></div>
<div class="col-md-4">
    <ul class="list-group">
        {{#each employees}}
        {{#if dataValues.Admin}}
        <li class="list-group-item d-flex justify-content-between align-items-center">
            {{dataValues.id}}.
            {{dataValues.Name}}
            <span class="badge badge-primary badge-pill">ADMIN</span>
        </li>
        {{/if}}
        {{/each}}
    </ul>
</div>